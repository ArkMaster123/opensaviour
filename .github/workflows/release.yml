name: release

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Version to release (e.g., 1.0.0)"
        required: true
        type: string

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            build_os: linux
            targets: "linux-arm64 linux-x64 linux-x64-baseline linux-arm64-musl linux-x64-musl linux-x64-baseline-musl"
          - os: macos-latest
            build_os: darwin
            targets: "darwin-arm64 darwin-x64 darwin-x64-baseline"
          - os: windows-latest
            build_os: win32
            targets: "windows-x64 windows-x64-baseline"
    
    runs-on: ${{ matrix.os }}
    
    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.3.5"

      - name: Install root dependencies
        run: bun install

      - name: Install opencode dependencies
        run: bun install
        working-directory: packages/opencode

      - name: Build CLI
        run: bun run script/build.ts --os=${{ matrix.build_os }}
        working-directory: packages/opencode
        env:
          OPENCODE_VERSION: ${{ inputs.version }}
          OPENCODE_CHANNEL: latest

      - name: Create archives (Unix)
        if: runner.os != 'Windows'
        working-directory: packages/opencode
        run: |
          for target in ${{ matrix.targets }}; do
            dir="dist/opencode-${target}"
            if [ -d "$dir" ]; then
              echo "Processing $dir"
              # Rename binary from opencode to opensaviour
              if [ -f "$dir/bin/opencode" ]; then
                mv "$dir/bin/opencode" "$dir/bin/opensaviour"
              fi
              
              if [[ "$target" == linux* ]]; then
                tar -czf "dist/opensaviour-${target}.tar.gz" -C "$dir/bin" .
              else
                (cd "$dir/bin" && zip -r "../../opensaviour-${target}.zip" .)
              fi
            else
              echo "Directory $dir not found, skipping"
            fi
          done

      - name: Create archives (Windows)
        if: runner.os == 'Windows'
        working-directory: packages/opencode
        shell: pwsh
        run: |
          $targets = "${{ matrix.targets }}" -split " "
          foreach ($target in $targets) {
            $dir = "dist/opencode-$target"
            if (Test-Path $dir) {
              Write-Host "Processing $dir"
              # Rename binary from opencode to opensaviour
              if (Test-Path "$dir/bin/opencode.exe") {
                Rename-Item "$dir/bin/opencode.exe" "opensaviour.exe"
              }
              Compress-Archive -Path "$dir/bin/*" -DestinationPath "dist/opensaviour-$target.zip"
            } else {
              Write-Host "Directory $dir not found, skipping"
            }
          }

      - name: List dist contents
        run: ls -la packages/opencode/dist/ || dir packages/opencode/dist/
        shell: bash

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: binaries-${{ matrix.os }}
          path: |
            packages/opencode/dist/*.tar.gz
            packages/opencode/dist/*.zip

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: List artifacts
        run: find . -type f -name "*.tar.gz" -o -name "*.zip" | head -20

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ inputs.version }}
          name: OpenSaviour v${{ inputs.version }}
          draft: false
          prerelease: false
          files: |
            artifacts/*.tar.gz
            artifacts/*.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
